Fix the following issues from {{ fix_type }}:

## Feedback to Address

{{ feedback }}

## Tasks

1. **Read and understand** each issue listed above

2. **Fix each issue:**
   - Address ALL critical and major issues
   - Address minor issues if straightforward
   - Follow project conventions from `.gba.md`

3. **Run tests** after making changes

4. **Commit changes:**
   ```bash
   git add -A
   git commit -m "fix({{ feature_slug }}): address {{ fix_type }} feedback"
   ```

## Requirements

- Fix ALL critical issues (must fix)
- Fix ALL major issues (should fix)
- Ensure tests still pass after changes
- Do not introduce new issues
- Do not change unrelated code

## STOP CONDITIONS

Stop when you have:
- Fixed all critical and major issues
- Committed the fixes
- Verified tests pass
