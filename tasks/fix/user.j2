Fix the following issues from {{ fix_type }}:

## Feedback to Address

{{ feedback }}

## Tasks

1. **Read and understand** each issue listed above

2. **ACTUALLY fix each issue using Edit/Write tools:**
   - Use the `Edit` tool to modify existing files
   - Use the `Write` tool to create new files if needed
   - Do NOT just describe what should be done - MAKE the changes
   - Address ALL critical and major issues
   - Address minor issues if straightforward
   - Follow project conventions from `.gba.md`

3. **Run tests** after making changes to verify fixes work

4. **Commit changes:**
   ```bash
   git add -A
   git commit -m "fix({{ feature_slug }}): address {{ fix_type }} feedback"
   ```

## Requirements

- Fix ALL critical issues (must fix)
- Fix ALL major issues (should fix)
- MAKE ACTUAL CODE CHANGES - do not just analyze or suggest
- Ensure tests still pass after changes
- Do not introduce new issues
- Do not change unrelated code

## STOP CONDITIONS

Stop when you have:
- Fixed all critical and major issues (with actual code changes)
- Committed the fixes
- Verified tests pass
