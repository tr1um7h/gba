Execute phase {{ current_phase_index + 1 }} of {{ total_phases }}: `{{ current_phase_name }}`

## Current State

{% if is_resuming %}
**RESUMING FROM INTERRUPTION**

Read `state.yml` to check progress. Continue from where you left off - do not redo completed work.
Check `git status` and recent commits to understand current state.
{% endif %}

**All Phases:**
{% for phase in phases %}
- {% if phase.status == 'completed' %}[x]{% elif phase.status == 'in_progress' %}[>]{% else %}[ ]{% endif %} {{ phase.name }}{% if phase.commit_sha %} ({{ phase.commit_sha }}){% endif %}
{% endfor %}

## Tasks for This Phase

1. **Read design spec** to understand requirements for `{{ current_phase_name }}`

2. **Implement this phase:**
   - Follow project conventions from `.gba.md`
   - Run tests after changes
   - Commit with: `feat({{ feature_slug }}): {{ current_phase_name }}`

## STOP CONDITIONS

Stop when you have:
- Completed the implementation for `{{ current_phase_name }}`
- Committed the changes
- Verified the changes pass tests

**DO NOT** continue to the next phase - the orchestrator will handle that.
**DO NOT** create PRs or push to remote - the orchestrator will handle that after all phases complete.
