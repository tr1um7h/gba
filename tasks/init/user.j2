Initialize GBA for this repository.

**IMPORTANT: Execute each task step-by-step, completing one before starting the next. Do NOT use parallel tool calls - the API has concurrency limits that will cause failures.**

## Tasks

1. **Create directory structure:**
   ```bash
   mkdir -p .gba
   mkdir -p .trees
   ```

2. **Update `.gitignore`** - add if not exists:
   ```
   # GBA worktrees
   .trees/
   ```

3. **Analyze the repository** by reading README.md and exploring the file structure to understand:
   - Programming language(s) and frameworks
   - Build system and dependencies
   - Project architecture
   - Important directories

4. **Create `.gba.md`** with project documentation for AI assistants:
   - Project overview
   - Technology stack
   - Directory structure with descriptions
   - Development setup commands
   - Build & test commands
   - Key conventions

5. **Update `CLAUDE.md`** - add reference to `.gba.md` (create file if not exists)

6. **Create `.gba/config.yml`:**
   ```yaml
   agent:
     permission_mode: auto
   git:
     auto_commit: true
   review:
     enabled: true
   ```

## Output

After completion, summarize what was created and suggest running `gba plan <feature-slug>` to plan a new feature.
